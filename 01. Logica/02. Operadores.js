// Operadores de Asignacion

    x = y  //Asignacion -- (x=y)
        // Asigna un valor a una variable

    x += y //Asignacion de Adicion -- (x =x+y)
        //Asigna el resultado suma el primer valor con el segundo y lo asigna al primer valor

    x -= y //Asigancion de Sustraccion --(x=x-y)
        //Asigna el resultado de restar el segundo valor al primer valor y lo asigna al primer valor

    x *= y //Asignacion de Multiplicacion -- (x=x*y)
        //Asigna el resultado de multiplicar el primer valor por el segundo y lo asigna al primer valor

    x /= y //Asignacion de Division -- (x=x/y)
        //Asigna el resultado de dividir el primer valor entre el segundo y lo asigna al primer valor

    x %= y //Asignacion de Resto -- (x=x%y)
        //Asigna el resultado de dividir el primer valor entre el segundo y lo asigna al primer valor

    x **= y //Asignacion de Expònenciacion -- (x=x**y)
        //Asigna el resultado de elevar el primer valor al segundo y lo asigna al primer valor

    x <<= y //Asigancion de Desplazamiento a la Izquierda -- (x=x<<y)
        //Asigna el resultado de desplazar el primer valor a la izquierda el numero de veces que indique el segundo y lo asigna al primer valor

    x >>= y //Asigancion de Desplazamiento a la Derecha -- (x=x>>y)
        //Asigna el resultado de desplazar el primer valor a la derecha el numero de veces que indique el segundo y lo asigna al primer valor

    x >>>= y //Asigancion sin Signo de Desplazamiento a la Derecha -- (x=x>>>y)
        //Asigna el resultado de desplazar el primer valor a la derecha el numero de veces que indique el segundo sin signo y lo asigna al primer valor

    x &= y //Asignacion AND
        //Asigna el resultado de aplicar la operacion AND al primer valor y el segundo y lo asigna al primer valor

    x ^= y //Asignacion XOR
        //Asigna el resultado de aplicar la operacion XOR al primer valor y el segundo y lo asigna al primer valor

    x |= y  //Asignacion OR
        //Asigna el resultado de aplicar la operacion OR al primer valor y el segundo y lo asigna al primer valor

// Operadores Aritmeticos

    a + b //Suma
        //Suma dos valores

    a - b//Resta
        //Resta dos valores

    a-- //Decremento
        //Decrementa el valor de una variable en 1
        
    a * b //Multiplicacion
        //Multiplica dos valores

    a /  //Division
        //Divide dos valores

    a ** b //Exponenciacion
        //Eleva el primer valor al segundo

    a++ //Incremento
        //Incrementa el valor de una variable en 1
        
    a % b//Modulo
        //Devuelve el resto de la division de dos valores

// Operadores de Comparacion

    a == b //Igualdad
        //Compara dos valores y devuelve true si son iguales y false si no lo son

    a != b //Negacion de Igualdad
        //Compara dos valores y devuelve true si son diferentes y false si no lo son

    a === b //Igualdad Estricta
        //Compara dos valores y devuelve true si son iguales y false si no lo son. Ademas, compara el tipo de dato

    a !== b //Negacion de Igualdad Estricta
        //Compara dos valores y devuelve true si son diferentes y false si no lo son. Ademas, compara el tipo de dato

    a > b //Mayor Que
        //Compara dos valores y devuelve true si el primer valor es mayor que el segundo y false si no lo es

    a >= b //Mayor o Igual Que
        //Compara dos valores y devuelve true si el primer valor es mayor o igual que el segundo y false si no lo es

    a < b //Menor Que
        //Compara dos valores y devuelve true si el primer valor es menor que el segundo y false si no lo es    

    a <= b //Menor o Igual Que
        //Compara dos valores y devuelve true si el primer valor es menor o igual que el segundo y false si no lo es

// Operradores Logicos

    exp1 && exp2 //AND
        //Devuelve true si ambas expresiones son verdaderas

    exp1 || exp2 //OR
        //Devuelve true si al menos una de las expresiones es verdadera

    !exp1 //NOT
        //Devuelve true si la expresion es falsa y false si es verdadera




