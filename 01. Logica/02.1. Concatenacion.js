// Concatenacion de cadenas de texto
    //Declaramos las variables
    cadena1 = 'Hola Ernesto '
    cadena2 = 'Como Estas'

    //Almacenamos el resultado de la concatenacion de las dos cadenas en una nueva variable usando el operador +
    fraseResultado = cadena1 + cadena2 

    //Mostramos el resultado en consola
    console.log(fraseResultado)

// Concatenacion de numeros
    //Declaramos las variables
    numero1 = 1
    numero2 = 7

    //Almacenamos el resultado de la concatenacion de los dos numeros en una nueva variable usando el operador + pero antes colocando un espacio entre ellos para indicar que no es una suma de valores sino una concatenacion de cadenas
    numeroResultado = numero1 + ' ' + numero2

    //Mostramos el resultado en consola
    console.log(numeroResultado)

numeroResultado1 = `el primer numero es ${numero1} y el segundo es ${numero2}`

console.log(numeroResultado1)