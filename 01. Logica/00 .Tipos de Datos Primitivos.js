//Tipos de Datos

string = "Cadena de Texto"
number = 17
booleano = true

// Casos Especiales

undefined 
// Se utiliza cuando una variable no ha sido inicializada o cuando se intenta acceder a una propiedad inexistente en un objeto. Por ejemplo, si se declara una variable sin inicializarla, su valor será undefined

null
//Se utiliza para indicar explícitamente que una variable no tiene un valor válido. Por ejemplo, si se quiere vaciar una variable, se le asigna un valor nulo

NaN
//Se obtiene como resultado de una operación aritmética con un valor undefined. Por ejemplo, si se realiza una operación que debería tener como resultado un número, pero se ha hecho con un valor indefinido, el resultado será NaN.